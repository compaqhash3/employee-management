# Local dev (Docker Postgres via docker-compose.yml):
DATABASE_URL="postgresql://postgres:password123@localhost:5433/employeedb"

# Production (Vercel): set DATABASE_URL in the Vercel dashboard's Environment
# Variables instead of committing it here. If using Vercel Postgres (Neon),
# this is added automatically when you create the database from the Storage tab.

NEXT_PUBLIC_BASE_URL="http://localhost:3000"
